name: Analogni Zvuk API Scraper

on:
  schedule:
    # Pokreće se svaki dan u 03:30 po UTC vremenu (pola sata nakon Dancing Beara)
    - cron: '30 3 * * *'
  workflow_dispatch: # Omogućava ručno pokretanje na gumb "Run workflow"

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repozitorija
      uses: actions/checkout@v3

    - name: Postavljanje Pythona
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Instalacija potrebnih biblioteka
      run: |
        python -m pip install --upgrade pip
        pip install requests

    - name: Pokretanje API scrapera
      run: python scraper_analognizvuk.py

    - name: Commit i Push promjena
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git add analognizvuk_ploce.csv
        git commit -m "🤖 Automatsko ažuriranje: Analogni Zvuk (API Sync)" || echo "Nema novih promjena za spremanje"
        git push
